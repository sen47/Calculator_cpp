#pragma once

template<typename T>
T add(const T, const T);

template<typename T>
T subtract(const T, const T);

template<typename T>
T multiple(const T, const T);

template<typename T>
T divide(const T, const T);